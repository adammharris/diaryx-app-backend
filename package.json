{
  "name": "diaryx-app-backend",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.4.0",
    "@elysiajs/openapi": "^1.4.10",
    "@neondatabase/serverless": "^1.0.1",
    "better-auth": "^1.3.14",
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}
